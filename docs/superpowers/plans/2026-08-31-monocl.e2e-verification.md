# MonoCl end-to-end verification

Task 14, step 5. Recorded 2026-08-31.

Observations marked **(observed)** were made by Marc, at the keyboard,
looking at the running app — they are the reason this document exists and
they cannot be produced any other way. Items marked **(cross-checked)**
were corroborated afterwards against a source of truth. Items marked
**(outstanding)** have not been performed.

The app under test was built from `worktree-monocl-impl`, ad-hoc signed,
launched from `~/Library/Developer/Xcode/DerivedData/MonoCl-*/Build/Products/Debug/MonoCl.app`.

## Confirmed

| # | Criterion | Evidence |
|---|---|---|
| 1 | The app launches | **(observed)** running as PID 98635; signature valid on disk and satisfies its designated requirement |
| 2 | Menu bar only, no Dock icon, no window | **(cross-checked)** `lsappinfo` reports `ApplicationType = "UIElement"`, so `LSUIElement` is in effect |
| 3 | Three dots, in order | **(observed)** session, weekly, platform, left to right |
| 4 | The keychain authorization prompt appears | **(cross-checked)** `SecurityAgent` spawned as PID 98636 immediately after launch; **(observed)** granted, after which real values appeared |
| 5 | Real usage values are fetched and displayed | **(observed)** Session 23%, Week 21% |
| 6 | Hover shows all three indicators on separate lines | **(observed)** three-line tooltip |
| 7 | A nominal usage value renders monochrome | **(observed)** 23% and 21% both render as unfilled monochrome rings, being below the 75% warning threshold |
| 8 | A `minor` platform status renders amber | **(observed)** the platform dot is amber and filled |
| 9 | `status.description` is shown verbatim | **(observed)** "Partially Degraded Service"; **(cross-checked)** `status.claude.com/api/v2/summary.json` reported `indicator: minor`, `description: "Partially Degraded Service"` at the same time — identical, no paraphrase |
| 10 | `utilization` is not scaled | **(observed)** 23% and 21% are plausible subscription figures. The design originally multiplied this field by 100, which would have displayed 2300% and pinned both usage lights to critical permanently. Its absence in the running app is direct evidence the corrected decoding is what shipped |
| 11 | Both reset-time formats render correctly | **(observed)** the session window, resetting within twelve hours, shows `resets 16:39`; the weekly window, beyond twelve hours, shows `resets Fri 08:59`. Until this observation the weekday branch's output was exercised by a test but never asserted |

## Outstanding

None of the following has been performed. Each needs a person at the
keyboard, and none may be inferred from the code.

- **The Deny path.** Revoke MonoCl's access to `Claude Code-credentials`
  in Keychain Access, relaunch, click Deny, and confirm: the usage dots
  stay dim, the menu reads "Keychain access denied" with a Retry item,
  and **no further dialog appears for at least three poll intervals**.
  This is the criterion that matters most, because the failure it guards
  against — re-reading the keychain on a timer and raising a modal dialog
  every cycle — only manifests for a user who declines, which nobody does
  while testing. Note that an ad-hoc signature changes on every rebuild
  and a keychain grant is bound to the signature, so a prompt reappearing
  after a REBUILD is expected and is not this defect (`DEFERRED.md` item 3).
- **Sleep and wake.** The dots should blank until the next successful
  fetch rather than showing a value from before the sleep, and a prior
  Deny should survive the wake without re-arming the dialog.
- **Immediate threshold response.** Dragging the warning threshold below
  the current session percentage should turn that dot amber at once, not
  at the next poll up to sixty seconds later.
- **The critical state.** No usage window has been above 90% during
  testing, so the red state has not been seen rendered.
- **No token in the unified log.** `log show --last 10m --predicate
  'subsystem == "net.sinistral.monocl"' | grep -ciE 'sk-ant|Bearer|accessToken'`
  should report 0. The log was empty during this session, which is
  consistent with the app logging only errors, but an empty log does not
  exercise the redaction.

## Cosmetic finding, visible only in rendering

The platform line reads as a run-on: `Platform Partially Degraded Service`.
Labels pad to nine characters, and "Platform" is eight, so it receives a
single space where "Session" receives two and reads as a column. Legible,
but that line looks like prose rather than a label and a value. Not
recorded as a defect; noted because it is the kind of thing no test sees.
